package com.zuexx.forgekit.samples

/**
 * The one domain type this starter ships. It exists to prove the chain from a provider through
 * a view model to a screen is wired and tested — replace it with a real domain rather than
 * building around it.
 */
data class SampleResource(val id: String, val name: String)
